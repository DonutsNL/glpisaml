name: PHP Linter

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: PHP Syntax Checker (Lint)      
      uses: StephaneBour/actions-php-lint@8.2